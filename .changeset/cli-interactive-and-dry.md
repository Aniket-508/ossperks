---
"ossperks": minor
---

Interactive prompts for `search`, `show`, `check`, and `categories` via `@clack/prompts`; category flow lists programs after pick. Program tables show name and URL only; `open` uses `execa`; check command split into smaller modules; update notice and styling helpers refactored.

Underlying core package adds shared relative-time helpers and tighter fetch utilities used by the CLI.
